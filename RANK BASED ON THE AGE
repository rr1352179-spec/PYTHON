names = ["ragul", "shaffi", "marko", "siraj", "max"]
age = [12, 23, 33, 45, 34]

# Combine age and name
res = list(zip(age, names))

# Sort by age in descending order
res = sorted(res, reverse=True)

# Print ranking
for i in range(len(res)):
    print("Rank {}: {} years old - {}".format(i + 1, res[i][0], res[i][1]))Ṣ
